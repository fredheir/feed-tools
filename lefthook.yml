min_version: 2.1.12
glob_matcher: doublestar

pre-commit:
  parallel: true
  commands:
    biome:
      glob: "**/*.{js,jsx,cjs,mjs,ts,tsx,cts,mts,json,jsonc,css,html}"
      run: pnpm exec biome format --write {staged_files}
      stage_fixed: true
    prettier:
      glob: "**/*.{md,yml,yaml}"
      run: pnpm exec prettier --write {staged_files}
      stage_fixed: true
    eslint:
      glob: "**/*.{js,jsx,ts,tsx}"
      run: pnpm exec eslint {staged_files}
    gitleaks:
      run: just secrets-staged
    actionlint:
      glob: ".github/workflows/*.{yml,yaml}"
      run: actionlint {staged_files}
    repo-guards:
      run: just doctor-staged

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --config commitlint.config.cjs --edit {1}

post-merge:
  commands:
    sync-if-needed:
      run: just sync-if-needed

post-checkout:
  commands:
    sync-if-needed:
      run: just sync-if-needed

pre-push:
  parallel: true
  commands:
    typecheck:
      run: just typecheck
    tests:
      run: just test-fast
    dependency-shape:
      run: just deps-check-fast
    deadcode:
      run: pnpm run deadcode
