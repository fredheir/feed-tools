#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
cd "$ROOT_DIR"

if [[ -n "${REPO_STANDARDS_LOCAL:-}" ]]; then
  exec pnpm --dir "$REPO_STANDARDS_LOCAL" --silent --filter @markolo-research/repo-standards repo-standards secrets --root "$ROOT_DIR" --config repo-standards.config.json
fi

exec pnpm exec repo-standards secrets --config repo-standards.config.json
